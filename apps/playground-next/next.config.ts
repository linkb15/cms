import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // rewrites: async () => [
  //   {
  //     source: '/:path*',
  //     destination: '/',
  //   },
  // ],
  // transpilePackages: ['@linkbcms/core-tsdown'],
};

export default nextConfig;
